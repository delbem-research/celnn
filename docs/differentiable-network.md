# Differentiable CELNN

The canonical guide is now
[Train a differentiable CELNN with PyTorch](source/how-to/differentiable.md), with
exact signatures in the [Differentiable CELNN API](source/reference/differentiable.md).

This file remains only as a compatibility pointer for existing repository links;
canonical content lives under `docs/source/`.
